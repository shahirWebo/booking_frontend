name: turf_booking_api_client
description: Shared API transport boundary for Turf Booking apps.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  http: ^1.2.2
  turf_booking_core:
    path: ../app_core

dev_dependencies:
  test: ^1.25.0
