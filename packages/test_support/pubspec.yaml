name: turf_booking_test_support
description: Shared test helpers for Turf Booking apps and packages.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  flutter_test:
    sdk: flutter
  turf_booking_core:
    path: ../app_core
