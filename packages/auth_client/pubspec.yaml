name: turf_booking_auth_client
description: Shared customer and vendor session boundary.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter_secure_storage: ^9.2.4
  turf_booking_core:
    path: ../app_core

dev_dependencies:
  flutter_test:
    sdk: flutter
